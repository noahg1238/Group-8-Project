import React from "react";
import { View } from "react-native";

const settings = () => {
  return <View></View>;
};
export default settings;
/*settings page should have settings included on prototype plus possibly more.*/
