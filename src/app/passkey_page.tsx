import React from "react";
import { View } from "react-native";

const passkey_page = () => {
  return <View></View>;
};

export default passkey_page;
/* should have the ability to add or change passkey links from settings page */
